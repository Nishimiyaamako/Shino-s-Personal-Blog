# Backend target used by Vite dev proxy (/api, /uploads)
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:3001

# Optional absolute API base URL for preview/production.
# Leave empty to use same-origin requests.
VITE_API_BASE_URL=
